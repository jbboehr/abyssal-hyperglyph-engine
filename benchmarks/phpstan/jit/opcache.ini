; This directory is appended to PHP_INI_SCAN_DIR for both the PHPStan parent
; and its workers, so every process receives the same JIT configuration.
opcache.enable_cli=1
opcache.jit=tracing
opcache.jit_buffer_size=64M
